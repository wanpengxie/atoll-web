# TC-0302 — B-BR-04b first local live echo

## Contract

- **Baseline:** `TC-0302`, old `fae8b70:tests/browser/phase-b.spec.js:193`.
- **User capability:** after the first local Composer send, the live feed echo is reconciled into the same visible message identity; it does not create a second message or a new-dynamic/unread notice.
- **Invariant:** the submitted public `submit.payload.id` is the same identity carried by the public live `feed.payload.envelope.id`; the final Presentation exposes one `[data-message-id]` row and one exact message text.
- **Current public owner:** Composer submission over the workspace WebSocket and the canonical Conversation Presentation row. The browser contract uses only public DOM and WebSocket frames; it does not read diagnostics, React state, IndexedDB, or another private store.
- **Allowed change:** `tests/browser/tc0302-phase-b-live-echo.spec.js` and this report only. No product, mock, vendor, package, lockfile, or snapshot change.

## Uniqueness

The migration ledger retains one `TC-0302` row, whose only baseline reference is the absent `fae8b70:tests/browser/phase-b.spec.js:193` path. Before implementation, refs and registered worktrees were checked for `TC-0302`, `B-BR-04b`, and equivalent browser claims. `TC-0301` was already reserved by browser-a-f; `TC-0310` was already reserved by A-D. No competing `TC-0302` claim was found.

## Evidence

Base: `28bc0b832414f2a7fcc4df4351a9b296ffe49500` (`28bc0b8`). Independent worktree:

`/tmp/claim-tc0302-28bc0b8`

The test performs only public user actions and public observables:

1. reset the `real-backend-shape` fixture with the fae seed and log in as `root`;
2. fill the visible `消息` Composer and click the visible `发送` button;
3. capture the public WebSocket `submit` frame to obtain the submitted message id;
4. wait for exactly one public live `feed` frame carrying the same envelope id and canonical `payload.body.text`;
5. assert one visible `[data-message-id="<submitted-id>"]` row, one exact message text, and no `条新动态` button, including after a settling window.

Results on the independent worktree:

- Chromium focused run (`--repeat-each=3`): **3/3 passed**.
- Adjacent phase-B browser coverage (`tc0296`, `tc0299`, `f7-phase-b-0298`): **3/3 passed**.
- `npm run build`: **passed** (Vite completed; existing chunk-size warning only).

## Result

**PASS — the first local live echo preserves one public message identity and does not manufacture a new-dynamic notice.** This is a test/audit-only migration; no product behavior was changed.
