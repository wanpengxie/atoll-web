# TC-1488 — foreground wake reattach contract

Base: `82e22a21c660a6205cbe21a184ae2506bf060dae` (`refactor/frontend-subtractive-cleanup`)
Branch: `unit-a-d/tc1488-wire-wake`
Disposition: PASS by existing public contract; no duplicate test added.

| Case | User capability | Invariant | Current public owner | Setup → action → observable |
| --- | --- | --- | --- | --- |
| TC-1488 | When a mobile foreground wake arrives while the old WebSocket still appears attached, the client immediately reattaches and resumes from the newest cursor. | The reconnect owns a new generation; the attach uses the current channel cursor and focus, and the old generation cannot remain authoritative. | `createWire` in `src/net/wire.js`, through its public `wake` callback and attach payload. | Create a wire with `wake`, cursor `c0:3`, and focus `c0`; open and receipt generation 1; advance cursor to 9; invoke `wake('visible')`; verify a second socket exists immediately, generation 2 attach carries `since:{c0:9}` and `focus:'c0'`, and state records generation-1 foreground resync. |

## Evidence

- Focused public test already present at `tests/wire.test.js:274`: `npx vitest run tests/wire.test.js -t 'reattaches immediately on mobile foreground wake' --reporter=verbose` — 1 passed.
- Owner regression: `npx vitest run tests/wire.test.js --reporter=dot` — 1 file, 20 passed.
- No product or test source change was necessary; the ledger's historical `src/app/hooks/useChannelFeed.js` mapping is stale, while the runnable owner is `src/net/wire.js`.
- No skip, expected-fail, private export, compatibility layer, or second owner was introduced.

This report closes only the TC-1488 evidence gap. It does not claim adjacent TC-1487/TC-1489 cases or any Feed/notification product change.
