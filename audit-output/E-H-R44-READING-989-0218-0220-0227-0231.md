# E–H R44 exact `9896328` re-verification: 0218/0220/0227/0231

Date: 2026-09-20

Primary product candidate: clean detached `9896328` (`fix(feed): publish
fenced history page completions`). Product source was not edited in this
round. The later owner candidate `f004888` (`fix(reading): retain committed
viewport coverage`) landed while this round was in progress and was verified
in its own clean detached worktree; its result is reported separately from the
exact-`9896328` result below.

Contract: [`docs/TEST-MIGRATION-EXECUTION-CONTRACT.md`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/docs/TEST-MIGRATION-EXECUTION-CONTRACT.md)

Tests:

- [`f7-history-water-baseline-0217-0221.spec.js`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/tests/browser/f7-history-water-baseline-0217-0221.spec.js)
- [`f7-history-access-baseline-0227-0230.spec.js`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/tests/browser/f7-history-access-baseline-0227-0230.spec.js)
- [`f7-history-warm-cache-baseline-0231.spec.js`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/tests/browser/f7-history-warm-cache-baseline-0231.spec.js)

## Result matrix

| Case | Old user action and observable | Current public owner and invariant | `9896328` result |
|---|---|---|---|
| 0218 | Seed delayed deep history with 320 dense rows and two Claude rows; login; click the public Claude filter. The filter must silently scan physical pages until the target Claude row is visible, while demand is anticipatory and no foreground/confirmation UI appears. | [`channel-feed-runtime.js:1036`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/src/model/channel-feed-runtime.js:1036) owns one `history.batch_complete` for each current physical page. Every public completion must have a non-empty unique `ref` and unique `(scanLowSeq, scanHighSeq, nextBeforeSeq)`; `useHistoryConsumer` may start demand but cannot impersonate completion. | **PASS**, strict repeat-3 (`3/3`). At least two completions, non-empty refs, no duplicate refs/ranges, no foreground or confirmation state, and the target row all held. |
| 0220 | Set the public viewport to `1280×5000` and login after the deep-history reset. A committed under-filled list must publish coverage admission only after rows, both boundaries, current attachment, older supply, and bottom readiness, then start one history demand; replaying the same coverage must not publish it twice. | [`useBrowsingReadingController.js:121`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/src/ui/timeline/useBrowsingReadingController.js:121) owns the `viewport-coverage` gate and public event. The existing test keeps the positive geometry/identity assertions and requires the same latest public detail exactly once. | **REJECT** on exact clean `989`: no `history.viewport_underfilled` event within 10s. **PASS** on owner candidate `f004888`, repeat-3: retained coverage admitted after readiness and same public detail occurred once. |
| 0219 | Login, click the public Claude filter, observe exact partial empty text then exact EOF text; the timeline list must retain geometry and remain quiet without foreground/confirmation UI. | [`ConversationSurface.jsx:317`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/src/ui/conversation/ConversationSurface.jsx:317) owns the exact partial/EOF empty states; reading status is not a substitute. | **PASS** regression sample (1/1). Exact text, EOF transition, quietness, and unchanged geometry held. |
| 0227 | Open `c0.project`, revoke membership, observe inaccessible state and zero revoked freshness frames; grant membership, then observe the successor socket and exactly one grant-side `channel_meta` through the settling window. | Existing access/grant lifecycle calls [`refreshChannel`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/src/app/hooks/useWireSession.js:799), which enters the existing feed refresh/load owner at [`channel-feed-runtime.js:1177`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/src/model/channel-feed-runtime.js:1177). | **PASS** regression sample (1/1). Zero revoked frames, one successor socket, and exactly one grant-side frame held. |
| 0231 / FAE-1644 | Open huge history, warm a bounded durable cache, reload, then issue two native top gestures. Startup must have `128 ≤ rows < 1000`, no outstanding startup segment, one satisfied demand per gesture, and a strictly older second anchor. | [`channel-replica.js:847`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/src/model/channel-replica.js:847) owns the current `atoll-channel-replica-v1` durable cache; history demand owns the public wheel intents. The old `atoll-feed-v8` storage is not reopened. | **REJECT** at the first strict boundary: canonical `c0` rows stabilized at `42`, below `≥128` after 30s. The later reload/gesture assertions remain intact for a future owner candidate. |

0219 and 0227 are only regression samples in this round; their earlier exact
repeat-3 acceptance remains recorded in
[`E-H-R43-READING-EXACT-0BA-AND-WARM-CACHE-0231.md`](/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web/audit-output/E-H-R43-READING-EXACT-0BA-AND-WARM-CACHE-0231.md).

## Commands and results

### 0218 exact feed completion proof

```text
ATOLL_TEST_WEB_PORT=16760 ATOLL_TEST_MOCK_PORT=20160 npx playwright test \
  tests/browser/f7-history-water-baseline-0217-0221.spec.js \
  --grep='TC0218' --repeat-each=3 --reporter=line --workers=1
```

Result: `3 passed (37.4s)`. The strict test itself records the physical
completion count, refs, ranges, duplicate checks, cold snapshot, demand
fields, quiet UI, and target row in its public evidence attachment. The
`9896328` owner publishes the event after the same generation/attachment/current
fence used by the canonical commit, including cache continuation and network
pages; the test does not accept the internal `completedPages` counter alone.

### 0220 clean baseline and candidate probe

Clean `9896328`:

```text
ATOLL_TEST_WEB_PORT=16761 ATOLL_TEST_MOCK_PORT=20161 npx playwright test \
  tests/browser/f7-history-water-baseline-0217-0221.spec.js \
  --grep='TC0220' --reporter=line --workers=1
```

Result: `1 failed`; first strict boundary is missing
`history.viewport_underfilled` at the unchanged 10s wait.

The uncommitted shared coverage candidate was tested only in a disposable
worktree, repeated three times:

```text
ATOLL_TEST_WEB_PORT=16764 ATOLL_TEST_MOCK_PORT=20164 npx playwright test \
  tests/browser/f7-history-water-baseline-0217-0221.spec.js \
  --grep='TC0220' --repeat-each=3 --reporter=line --workers=1
```

The first uncommitted probe (before the owner commit) was `3 failed`: it did
replay retained coverage after Reading readiness, but emitted the identical
public detail twice after an intermediate geometry/range change. All three
runs failed `sameCoverage` with `Received length: 2`; that probe is retained
as a rejected pre-candidate witness and was not staged.

The owner then landed `f004888` with the admission key narrowed to mounted
root/presentation/viewport identity. Its clean acceptance run was:

```text
ATOLL_TEST_WEB_PORT=16767 ATOLL_TEST_MOCK_PORT=20167 npx playwright test \
  tests/browser/f7-history-water-baseline-0217-0221.spec.js \
  --grep='TC0220' --repeat-each=3 --reporter=line --workers=1
```

Result: `3 passed (22.0s)`. The strict test observed a positive attached /
current / bottom-ready / older coverage detail, one intent, and one event for
that same public detail in each run. This closes the 0220 owner contract on
`f004888`; it does not retroactively change the exact-`9896328` result.

### 0219/0227 regression sample

```text
ATOLL_TEST_WEB_PORT=16762 ATOLL_TEST_MOCK_PORT=20162 npx playwright test \
  tests/browser/f7-history-water-baseline-0217-0221.spec.js \
  tests/browser/f7-history-access-baseline-0227-0230.spec.js \
  --grep='TC0219|TC0227' --reporter=line --workers=1
```

Result: `2 passed (19.5s)`. These results are samples only and do not replace
the earlier repeat-3 evidence.

### TC0231 warm-cache first boundary

```text
ATOLL_TEST_WEB_PORT=16763 ATOLL_TEST_MOCK_PORT=20163 npx playwright test \
  tests/browser/f7-history-warm-cache-baseline-0231.spec.js \
  --reporter=line --workers=1
```

Result: **RED** after the 30s poll: expected at least `128` canonical
`atoll-channel-replica-v1` rows for `c0`, observed `42`. The test's `finally`
attachment still records startup diagnostics; the old transport receipts are
not treated as cache completion or as a reason to weaken the bound.

## Disposition

0218 is closed as a strict PASS on the `9896328` feed owner. 0219 and 0227
remain exact PASS regression samples. 0220 is **PASS on the clean owner
candidate `f004888`** after repeat-3; exact clean `989` remains recorded as
the pre-owner missing-event result. 0231 remains a strict warm-cache
regression at the canonical durable-row boundary. Existing 0218/0220 test
contracts were not relaxed, and no product source was changed by this agent;
no private export, legacy API, skip, deletion, compatibility owner,
vendor/package, or lockfile was changed.
