# Frontend handoff — stopped at user request

This is an **unfinished work snapshot**, not a release or a claim of full acceptance.
The user requested all work stop, a handoff document, and a commit. Running child
agents were interrupted; timer list was empty. Do not resume work without the new
owner/user's direction. No deployment or real-service restart was performed.

## Repository and authority

- Repository: `/home/xiewanpeng/.atoll/device/daemons/local-device/channels/c0.dev/atoll-web`.
- Branch: `refactor/conversation-frontend-r3`.
- Prior snapshot: `d45040038ce8d043524af8ab6c8dc7649160baf5`.
- HEAD before this handoff: `5409ada` (Claude's user-action-only probe fix). Preserve it; probe storm work belongs to Claude, not this list/notification effort.
- The handoff commit saves current tracked source/test/doc changes and the four new source/test files. Generated browser output, profiles, and evidence directories remain on disk, not staged wholesale.
- Installed Virtuoso ESM SHA256 at handoff: `87560b6dc8dbfda5b88b2df457ca2bf735a0e58c97da7e0635a1c3f828620742`.
- `LegendMessageList.jsx` actually wraps Virtuoso. The unused Legend dependency/name is a known cleanup item, not a second active runtime.

## Highest priority: latest user contract and open failures

The user still reports notifications that cannot be cleared, top-of-history stalls,
and apparent missing messages after rapidly scrolling to top then back to bottom.
Do not dismiss these reports using passing fixture results.

**Latest notification correction, NOT yet proven implemented:** reaching the latest
content in the current scope (including a successful jump-to-latest) acknowledges
the known/installed backlog in that scope. Do not require every long message to
pass through the viewport. Browsing can still acknowledge individually seen items.
Filtered-tail acknowledgement must not clear other scopes; future arrivals must
not be swept into an older receipt. Do not clear before the jump actually succeeds.
The previous assistant repeatedly overclaimed completion based on exact-row tests.

## Current source changes saved here

### Notifications — candidate, not finally accepted

- New `conversation-visibility.js` / `notification-policy.js`; rail and viewport
  consume shared semantics with different eligibility: rail request/readable final,
  viewport additionally independently readable events. Activity timer/dot is a
  separate lifecycle, not unread.
- Exclude progress/processing, status-only terminal, UI/system/control and nested
  tool lifecycle. Preserve standalone user-facing structured/failed results and
  timer results with actual readable content. A visible row alone is not a reason
  to notify. Parent/type heuristics need ongoing business review, not blind trust.
- FeedCache notification context restoration includes parents and read boundary;
  inactive-channel restoration goes into existing Replica, not a second unread
  store. Incomplete context displays pending/unknown instead of false zero.
- Short-list visibility: formal List commit schedules the existing observation;
  do not accept ancestor/background hits, hidden/preparing DOM, or mere mounting
  as read evidence. Early ancestor and retry proposals were rejected.
- Reported bounded results: focused 224, MessageList/cursor 89, notification browser
  2/2, F7 8/8, build. These predate the latest user-level tail-clear correction and
  are NOT whole-system acceptance.
- Remaining ResizeObserver error was captured from installed Virtuoso observer
  on `.timeline-input-resize-content`; content rect 2699 vs border rect 3443,
  scrollTop 1423 then 1516→1423. Root cause/fix not closed. Temporary diagnostic
  wrapper was to be removed; owner was interrupted, inspect final diff before use.

### Waiting

- Keep `retainTerminalClosure` when trimming matched closed turns. Removing it
  resurrects queued via: old history page buffered → live matched terminal → trim
  → old pageEnd releases request/queued. Sorting each reservoir newest-first does
  NOT prove safety across these sources.
- New `tests/waiting-closure-entry.test.jsx`: six real-entry tests, independent
  review passed; related 103 tests/build passed. Memory-window change is comments,
  not removal of the protection. Cache test background errors are asserted empty.
- Compact closures are unbounded within ChannelState lifetime (~453 bytes/item
  measured). No safe cap/TTL/reclamation proof exists. Body/error completeness
  after trimming remains a separate open issue. Do not remove safety to get zero
  closures or present scan coverage as lifecycle completeness.

### Input and sending

- Current Surface/CSS/Legend input smoothing: seven height targets/seven existing
  issuer writes, about 5px maximum frame movement; bounded 98 tests/build passed.
  It uses coordinated input/spacer/presentation geometry; fixed48 is unchanged.
  This does NOT certify send/append or every mobile/IME case.
- Official installed 875 can temporarily remove a same-key tail from formal flow
  during revision/viewport change, shrinking extent by 228px and native-clamping
  scrollTop. App compensatory write experiments were rejected.
- Frozen **isolated** candidate `440c6f4d` fixes that extraction; NOT installed.
  Clear phase height3895/gap0/no writer; append +366 then existing App writer gives
  final gap0. First rAF still observed gap366 before writer (~28ms); same-paint/no
  visible jump was NOT proven. 238 pass/4 skipped, type/lint/dual build reported.

### History, apparent missing messages, performance

- Installed875 startup and one original continuous mixed-height compositor track
  passed. Do not inherit every old candidate's late-media/width/selection gates.
- Rapid top→bottom ordinary/sparse and DEV/prod clean tracks did not lose baseline
  IDs and restored visible tail158–160. This does not close the user's live report.
- 19 history intents were **legitimate different frontiers**, not duplicate storms.
  Do not deduplicate them away or suppress new gestures.
- Fixed-revision return-to-bottom hotspots: vendor formal **layout effect** repeated
  full-children scans/measurements (NOT RO callback), and Timeline rowRenderRevision
  dependency-array/JSON rebuilding. Diagnosed, not fixed.
- Content-plan unpublished bounded candidate cache + stable Markdown components +
  weak rendered-tree reuse landed. Same-track max long task357→144ms, 60 focused
  tests/build. Cold production preview274ms vs DEV593–738ms distinguishes DEV cost,
  not proof user DEV experience is fixed. Overscan removal A/B rejected.
- Scope/late-self preference fix is in working snapshot with targeted tests; no
  complete integrated browser acceptance. A→B→A baseline repeat8 passed; old +329
  was a different library artifact and was wrongly called a current failure.

### Smooth historical reveal — NOT integrated

Two isolated implementations exist. Row height animation caused reverse library
compensation. Final-extent same-DOM FLIP also failed without library coordination.
Last hypothesis: hold existing deviation=H/scrollTop=H, animate same List -H→0,
then atomically normalize deviation/scroll at end. This is **unproven**, not an
approved final design. No second scroll writer/timer chase; no opacity-only claim.
The user explicitly requests Loading→real smooth downward content entry.

## Evidence and isolated work (local, not all committed)

Channel root is the parent of this repository. Important files:

- `../audit-output/SYSTEM-BUSINESS-BOUNDARIES-CURRENT.md`
- `../audit-output/ALL-TASK-DELIVERY-MATRIX-d450400.md` (baseline matrix, not current closure count)
- `audit-output/EXECUTION-HISTORY-RAW-INDEX-20260918.md` (raw ~/.codex session index)
- `audit-output/NOTIFICATION-LIFECYCLE-OLD-VS-CURRENT.md`
- `audit-output/notification-policy-current/RESULT.md` and `SERVED-SOURCES.sha256`
- `../audit-output/TOP-BOTTOM-MISS-CURRENT-DIAGNOSIS.md`
- `../audit-output/TOP-BOTTOM-MISS-MATERIALIZATION-PROFILE.md`
- `../audit-output/waiting-scroll-installed-875-15813/INPUT-SMOOTHING-VERDICT.md`
- `../audit-output/formal-tail-flow-440c6f4d/MANIFEST.md`, `DELTA.md`, and package dist.
  ESM SHA256: `440c6f4d1bcbfc00caf54ab0fe6a604e41c9a30c45abb0e2123a6c7f66a51aa0`.
- `audit-output/send-flow-impl440-hardbound-15783/` original send browser evidence.
- `../formal-tail-flow-candidate/` isolated library working tree; not workspace dependency.
- `../waiting-closure-candidate/` isolated rejected-then-corrected closure experiment.
- `/tmp/atoll-history-reveal-prod-JIrd37`, `/tmp/atoll-history-reveal-flip-Vt1PJ2`:
  rejected/unaccepted animation candidates. Temporary paths are not durable delivery.

Raw sessions under `/home/xiewanpeng/.codex/sessions` contain original executions;
use the index rather than treating repeated status messages as proof.

## Acceptance boundaries and next owner

No current frozen combined unit+browser+fuzz+build acceptance exists. Baseline
`d450400` had independently rerun 994 units, but this snapshot has further changes.
No new full suite is being launched during handoff. `git diff --check` was clean
before document creation. This commit is preservation, not certification.

Unfinished broader areas include input variants/real devices, full permission
journeys, fold/content/selection combinations, history source fidelity (backend
scope), completeness, legacy dependency/name cleanup and documentation consistency.
Do not add arbitrary 100k/pixel-perfect-restore budgets the user cancelled.

Work must remain stopped now. If a new owner is authorized to continue, prioritize
the latest actual notification clear complaint and installed top/missing/send
behavior. Bind evidence to served module bytes; past false greens used stale Vite
prebundles, mounted-not-visible assertions, wrong fixture seeds, and incomplete
scope. Preserve counterexamples and unknowns; never concatenate partial greens
into whole-system completion.
