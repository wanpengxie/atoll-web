> R3说明：保留原始反馈编号、来源与旧审核记录。以下历史完成结论和后端/组件实施决定不作为当前授权；当前裁决见 CONVERSATION-FRONTEND-REBUILD.md 与 CONVERSATION-ARCHITECTURE-SCENARIO-AUDIT.md。历史分页统计由此前审核记录提供，本轮没有重新查询账本。新增“恢复后消息不见”见 R3 的 F29/N01。

# 用户反馈总账：原始记录 → 行为合同 → 反例轨迹 → 验收

日期：2026-09-16。状态：历史反馈核对完成；不是实现完成或用户验收通过。

**本表是证据索引，不是行为模型。** 不以逐字分类原句作为设计完成判据。
主方案第1.8节按J1–J7完整使用过程解释这些反馈：进入与获取内容、持续追新、历史阅读、内容变化、
输入发送、Surface往返、当前任务控制。每个过程都包含前置条件、触发、系统推进义务、失败恢复和可见完成条件。
例如“进入频道看不到消息”可能是初始数据未请求、缓存/连接屏障、授权失败、投影未发布或目标不可用，
不能未经证据就等同于“空列表渲染bug”；“不会拉新内容”要检查同步义务是否建立并持续推进，不能只检查某次API调用存在。

## 1. 核对范围与证据规则

- 当前频道：`5473ce43-bd17-4e78-859e-08befae851d8`。
- 固定查询快照：`head_seq=56959`；范围从 2026-09-13 00:00（UTC+08）至该快照。
  为避免“这三天”的自然语言边界遗漏，覆盖 9 月 14–16 日并向前扩到 13 日。
- 先按 day 查询统计概览，再以 sender_kind=human、同一时间范围/快照逐页检索。
  共走完 24 个检索页，最后 has_more=false；空的 scan_limited 页也继续翻完。
- 返回 201 条不重复 human 消息，其中一条 superseded 正文已由 replacement_text 取代。
  这不是 201 个 bug；包括需求、追问、重复反馈、操作约束及其他主题。
- 唯一截断的用户长文 #54230 已用 read_seq 完整读取；它引用此前助手分析，是用户要求保留的设计输入，
  不把里面每条助手推测重新标为用户亲自观察到的故障。
- #48748 围观问题、#50083 CI 问题取了不带筛选的上下文以辨明归属；上下文邻近不当作因果关系。
  边界外 #48721/#48747 超大单条消息分页问题仅作为补充依赖列出，不混入窗口内统计。
- 下表 seq 是当前频道账本定位依据，可用 `system.log.query(read_seq, head_seq)` 复核。
  仅覆盖本频道可读取的反馈，不声称搜索了其他频道或私下讨论。

先前助手说“修好了”“测试通过”“已提交”不是本次关闭问题的证据。
同一症状再次出现的用户报告优先于先前完成声明；未知是否复现的具体根因不强行归为一个原因。

## 2. 前端直接反馈与明确交互要求

轨迹 ID 来自 `CONVERSATION-ARCHITECTURE-SCENARIO-AUDIT.md`。
“原32轨迹覆盖”仅指本轮核对前的推演，不等于组件实现已通过。

| ID | 用户实际反馈/要求（合并重复，不合并不同症状） | 原始 seq | 原32轨迹覆盖 | 责任与补齐后的验收判据 |
|---|---|---|---|---|
| F01 | 新构建网页更卡，尤其打字响应比 tag 慢，疑似 CPU 卡顿 | 49133、49220 | 缺专门输入响应轨迹；不能拿 D05 滚动性能替代 | Composer 订阅隔离 + 增量 Presentation；新增 E01，history/live/解析负载下输入法与打字延迟独立计量 |
| F02 | 移动 Header 又变厚，之前修过的布局回归 | 50669 | 遗漏该回归 | MobileSurface/Header；新增 E02，首页↔会话↔预览后的既有紧凑头部尺寸不被会话样式污染 |
| F03 | 移动 Preview 不适配屏幕，宽度无法使用 | 50766 | A05 仅覆盖返回定位，未覆盖预览本身 | PreviewSurface；新增 E03，视口适配、横竖屏、内容溢出及关闭返回分开验收 |
| F04 | PC 消息页面偶尔闪烁，此次修改重新出现 | 50778、50788 | D05 只覆盖快速滚动，未覆盖静止画面 | Presentation/列表提交；新增 E10，静止与慢滚时 live/history 更新都不能清空或重挂整窗 |
| F05 | 持续上滑仍跳、不丝滑；大跳改善后快速滑仍有小抖 | 50984、51486、51656、53402、56014、56041、56393、56408 | B01、D01、D05 | MessageList/内容；无输入时测锚点偏差，有输入时分离原生位移和异常位移；不能只测最终位置 |
| F06 | 对话展开再收起，留下一大片空白 | 52742 | D02 只有展开回收，不含残留几何 | Choices/列表测量；新增 E04，同屏展开→收起及回收后收起均无过期占位 |
| F07 | 上下滚会卡住；阻塞感变严重；继续上滑却倒退几行 | 52749、52783、53014 | B01/D05 部分覆盖，未明确可滚范围与输入阻塞 | MessageList/输入路径；E04、E05，数据已存在仍须能滚，区分合法边界与主线程阻塞/错误高度 |
| F08 | 触到上滑边界一直弹 | 53046 | B09/D06 部分覆盖 | ReadingSession + HistoryDemand + 列表；E05，触顶反复输入不触发反复导航或补偿振荡，耗尽后安静停住 |
| F09 | 上滑到底不拉旧消息就看不到；已有调度仍临时等待 | 53298 | D06 仅证明可等待，不足以证明提前供给 | Scheduler；E05，提前需求/优先级、可用缓存命中、网络未到与渲染不及分开，不能以禁止加载止抖 |
| F10 | 移动发送很卡、等很久；感觉没有和后端建立联系 | 53215、53276 | C05 只覆盖身份确认，未覆盖接受延迟 | Submission/SyncSession/主线程；E01、E08，输入接受与网络状态独立；持久失败不清稿，传输失败可重试 |
| F11 | 手机新消息 push/打开后同步很慢 | 53436、55167 | A06 覆盖恢复，正常会话缺专门新鲜度轨迹 | SyncSession/Meta/Scheduler；E08，既有连接无新 push 也主动发现并追平缺口，不借重挂列表同步 |
| F12 | 切频道首次滑一下抖，之后稳定 | 54360 | A01/A03 有模型但缺冷测量验收 | MessageList；冷进入、首次真实手势与热态分别验证，不以热快照通过代替冷挂载 |
| F13 | 强刷/服务重启后快速翻历史闪并跳转 | 54295、55070、55102 | A01/A06/B01 | 会话不因同步重建；未知高度 prepend 仍是 G3 组件能力门槛，不算已关闭 |
| F14 | 有时新消息进来会抖一下 | 54360 | B02、C04 | 提交时阅读授权 + 列表；browsing 不动、following 正常跟随，两种状态都要验证 |
| F15 | 长文上下滑抖；最后一条更明显；同步结束又似乎消失 | 54928、54933、54951、54960 | D01 | 内容与阅读；同时覆盖行上方变化/行内部变化/终态替换；用户的原因猜测不等于已证明 latest 是唯一根因 |
| F16 | 上滑闪；快速从底部往回拉先空白再有内容 | 54390、56451 | D05/D06 | 数据准备、主线程、列表物化分别记录；已取得内容不消失，不让网络为所有空窗背锅 |
| F17 | 历史中触顶拉取后改变位置，持续滚动被抢定位 | 56014、56041、56556、56558、56610 | B01、B05、C04 | 用户运动优先，历史无导航权，G1/G3 执行能力仍须闭合 |
| F18 | 已滑到底，切出再进入却回到中间 | 54692 | A04 | 持久阅读 mode 权威，following 不恢复旧物理快照；已修订设计，不冒充代码已验证 |
| F19 | 等待区被改成每次收起，输入框/等待区样式被擅改 | 54558 | D02 只覆盖消息选择，不覆盖等待区默认/外观 | ComposerStack/Choices；E06，不改变既有默认展开和外观，运行状态不当作 UI 偏好 |
| F20 | 离线/未连接的状态文案把输入框向上推 | 54978、55203 | D04 涉及环境但未明确状态栏切换 | 固定 StatusBar；E06，相同输入内容遍历连接/排队/错误文案不改变输入框底边/外框占位 |
| F21 | 悬浮后消息区透到输入框下面，末条看不见、按钮点不了 | 55133 | D04/G6 | Surface 真实裁剪/点击区域；不是 Footer 假留白；默认态及输入栈增长态都验收，G6 不得仅标32px解决 |
| F22 | 回复消息没有使输入框自然增高 | 55203 | U13/D04 泛化覆盖 | Composer；E06，回复/附件/多行输入改变栈内高度，不被“固定状态栏”错误锁死 |
| F23 | 输入框增高但等待区静止，未贴在输入框上方 | 55203 | D04 泛化覆盖 | 同一 ComposerStack；E06，等待区跟随 Composer 上沿，不固定到页面坐标 |
| F24 | 外消息区底边应比输入框基础上沿高32px | 55260 | D04 没有检查明确32px约束 | Surface；E06 明确默认态32px；不可把该数值当任意浮层增长下都无遮挡的证明 |
| F25 | 移动频道列表页被消息页改坏 | 55730、55734 | 原32轨迹遗漏；主设计只有文字约束 | MobileNavigationSurface；E07，隐藏会话不参与目录布局/命中/焦点，来回进入后仍成立 |
| F26 | 缓存恢复过期等候状态，已结束任务重新出现 | 55755、55788 | C03 只覆盖乱序，未覆盖冷缓存回放 | ControlProjection；E09，缓存request/queued + 缺terminal不能推出current；G5仍需权威完整性 |
| F27 | 不仅前台恢复，进入/重选频道就应最高优先级查Meta、主动追新 | 55169、55320 | U01有规则、A06仅恢复，未独立验证 | SyncSession/Scheduler；E08，普通进入/重选、同连接无push、快速切换旧应答都覆盖 |
| F28 | 不能站内预览至少提供下载按钮，不能让用户回去找原链接 | 53633 | 原32轨迹没有下载动作；D07只是降级原则 | FilePreview；E03，unsupported/error态均保留可用下载来源，不影响返回书签 |

## 3. 用户重新贴出的架构输入：保留，但不伪造为直接故障报告

#54230 是完整的原设计引用与落实要求。下列条目必须留在实现总账，不能因为这轮只改视觉就宣称它们完成：

| 要求/已识别风险 | 归属 | 推演及关闭条件 |
|---|---|---|
| attach/live 与 IndexedDB/历史等待解耦 | SyncSession | A01/A06/E08；独立状态及失败路径，不仅改布尔名称 |
| 声明式历史需求，UI只传用户兴趣/范围/优先级 | HistoryDemand/Scheduler | B01/D06/E05；UI不执行自己的批次循环或另设网络调度器；#51989/#51991也明确要求 |
| 所有展开、线程、媒体、字体变化统一高度合同 | 内容/MessageList | D01/D02/E04；G4块内锚点能力不足不能用行顶通过代替 |
| IndexedDB outbox、本地接受、local echo、receipt/feed去重 | Submission | C05/E01；持久失败、离线、重试、乱序确认，不仅“清空输入框快了” |
| 增量 Presentation，避免结构变化扫描全部消息 | Presentation | D05/E01/E10；实际失效范围和主线程预算，不仅稳定React key |
| 切频道/筛选/Preview的统一导航，阅读与草稿持续性 | ReadingSession/Composer | A02–A05/C06/E03；本地恢复、跨账号、跨tab、跨设备分别定义，不能用内存Map概括全部 |
| 所有故障映射合同并先审查后集中验证 | 工程流程 | 本总账 + 场景审查 + 实现映射；没有组件能力就不能写已完成 |

## 4. 同期其他问题：不漏记，也不假装由消息列表重构解决

| ID | 原始反馈/上下文 | 归属与边界 |
|---|---|---|
| X01 | #48748：围观频道失效；#48782随后要求该问题先不解决 | 订阅/访问能力与gateway；旧回复#48751说observer无成员SubjectID，历史/live不可读。是历史分析，不当成本轮重新验证结论；不擅自重启该独立工程 |
| X02 | #49133之前的边界上下文#48721/#48747：超大单条消息会堵分页cursor | LedgerView/传输分页；邻近记录报告已修，本轮未重新验证。仍应有“单条超限可推进、不无限重试”协议合同，前端不可用滚动补偿修复 |
| X03 | #50083、50127、50157：CI错误、rg缺失及测试时序疑问；#51132/#51210追问未提交与失败测试 | 工具链/测试生命周期；按实际失败分类，不能随意删断言后称业务完成；本轮不运行CI或远端写操作 |
| X04 | #53387服务被操作中断；#53398用户自行恢复并要求停止操作 | 执行权限/发布流程；禁止在当前会话直接停承载频道的宿主服务；不是视觉层bug |
| X05 | #53458–#53608：36.5GB日志、构建重试风暴、reconcile不可见不可控 | ActorHost/Atoll管理平面；退避不等于完成，永久失败需要结构化可见可控。独立后端工程，不擅自删日志、改旧频道或restart |
| X06 | #50217–#50370、#50722等配置化/身份/生命周期设计讨论 | 独立系统设计主题，不伪装成新的消息区故障，也不能把其先前讨论当本轮执行授权 |

## 5. 交付约束也是反馈，不能从bug表里遗漏

- #51523：不能反复破坏既有页面/视觉优化；必须检查相邻Surface，不仅当前消息区。
- #53205/#53213/#53217、#54038/#54045：停止无意义频繁测试，先整体设计/重构，再集中验证。
- #54192/#54230/#54731/#54911：保存正确总设计、按文档对账；模糊测试覆盖真实状态机而非随机输入数量。
- #51146/#53413/#55037/#55057：提交与工作区状态必须如实交代；不能把构建等同于commit。
- #56091及之后：实际替换错误机制，不是拆文件；成熟组件边界不能被应用控制器再次抢占。
- #56909/#56911：用户不承担枚举需求、发现每条回归和第一轮测试的职责。

## 6. 本轮补齐与下一步责任

先按场景检验完整使用过程，再用本表确认没有遗忘历史回归；不能反过来只让每行故障对应一段补丁。

1. 补入 E01–E10 专项连续轨迹，覆盖原32轨迹遗漏/只泛化提到的交互；审查共42条，不称为42个运行测试通过。
2. 主方案新增相邻页面、输入响应、已用功能降级、任务新鲜度的明确边界，避免只盯消息滚动。
3. 六个未关闭门槛 G1–G6 仍是实施阻断项，不因为历史反馈已经全部归档就自动关闭。
4. 每项关闭必须同时记录：实际代码映射、验收证据、仍有的限制；原始用户seq与轨迹ID持续保留。
5. 历史问题可以被同一根本合同覆盖，但不同验收现象不能互相替代：
   不跳≠不闪、不闪≠可滚、可滚≠打字及时、输入稳定≠末条可点、缓存恢复≠后端队列新鲜。

当前结论：指定窗口内本频道的反馈已逐页核对、按范围归属；过去的32条推演没有完整覆盖它们。
补齐后均有记录与验证入口，但代码修复、组件能力、数据协议和真实设备验证仍需各自证据。
